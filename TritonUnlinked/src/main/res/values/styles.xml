<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo">
        <!-- Customize your theme here. -->
    </style>

    <style name="FullscreenTheme" parent="android:Theme.NoTitleBar">
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">@style/ButtonBar</item>
        <item name="metaButtonBarButtonStyle">@style/ButtonBarButton</item>
    </style>

    <style name="ButtonBar">
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:background">@android:drawable/bottom_bar</item>
    </style>

    <style name="ButtonBarButton" />

    <style name="BrowseActionBarTheme"
        parent="android:Theme.Holo">
        <item name="android:actionBarStyle">@style/BrowseActionBar</item>
        <item name="android:actionBarTabStyle">@style/BrowseTabs</item>
        <item name="android:textColor">@color/black_overlay</item>
    </style>

    <!-- ActionBar styles -->
    <style name="BrowseActionBar"
        parent="android:Widget.Holo.ActionBar.Solid">
        <item name="android:background">@color/triton_blue</item>
    </style>

    <!-- ActionBar styles -->
    <style name="BrowseTabs"
        parent="android:Widget.Holo.Tab">
        <item name="android:background">@color/triton_light_blue</item>
    </style>

</resources>
